# Driver-Drowsiness-Detection
A computer vision-based safety system that detects driver fatigue in real-time. It monitors Eye Aspect Ratio (EAR) using Dlib's facial landmarks and triggers a looping audio alarm via Pygame when drowsiness thresholds are exceeded.
